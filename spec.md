### Website design

Landing page with:

- previous runs
    - view run results
    - delete run
- new run
    - upload tiff stack
    - output into previous run sections


Pipeline:
    input: brightfield tiff stack (uncropped)
    input: gfp tiff stack (uncropped)
    clean out structures 


output of each run:
    - filament segmentation (masks)
    - filamnet classification (length, etc)
    - filament lifecycles 